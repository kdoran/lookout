const {RemoteCouchApi} = require('./remote-couch-api')
const {StoreApi} = require('./store-api')
const {PouchAdapter} = require('./pouch-adapter')
const {EntityApi} = require('./entity-api')

module.exports = {RemoteCouchApi, StoreApi, EntityApi}
