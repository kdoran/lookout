const App = require('./app/app')
const {
  Model,
  PouchDB,
  CouchServer,
  PouchAdapter,
  PouchRelationsAdapter
} = require('./api')

module.exports = {
  App,
  Model,
  PouchDB,
  CouchServer,
  PouchAdapter,
  PouchRelationsAdapter
}
